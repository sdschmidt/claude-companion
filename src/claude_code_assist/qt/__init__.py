"""Cross-platform desktop companion built on PySide6 (Qt6).

Roadmap and constants live in ``MILESTONES.md`` at the repo root —
that's the canonical source for what's done, what's next, and the
numeric tuning values ported from the Swift reference implementation.

Run standalone::

    python -m claude_code_assist.qt
"""

from __future__ import annotations

__all__: list[str] = []
